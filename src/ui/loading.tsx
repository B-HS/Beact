const Loading = async () => {
    return {
        default: () => (
            <div style={{ padding: '2rem', textAlign: 'center' }}>
                <p>Loading...</p>
            </div>
        ),
    }
}

export default Loading
